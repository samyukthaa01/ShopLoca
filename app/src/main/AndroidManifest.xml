<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shoplo">

    <application
        android:name=".ShopLocaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShopLo"
        tools:targetApi="31">

        <activity
            android:name=".activities.RegisterActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add ChatActivity -->
        <activity
            android:name=".activities.ChatActivity"
            android:exported="false"
            android:label="ChatActivity">
            <!-- You can add intent filters if needed -->
        </activity>

        <!-- Add SearchUserActivity -->
        <activity
            android:name=".activities.SearchUserActivity"
            android:exported="false"
            android:label="SearchUserActivity">
            <!-- You can add intent filters if needed -->
        </activity>

        <!-- Add ManagingActivity -->
        <activity
            android:name=".activities.ManagingActivity"
            android:exported="false"
            android:label="Shoploca">
            <!-- Add intent filters if needed -->
        </activity>
    </application>

</manifest>
